{
  "name": "hylian",
  "version": "1.0.0",
  "description": "A digital signature and contract management platform",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm run dev",
    "build": "npm run server:build && npm run client:build",
    "server:build": "cd server && npm run build",
    "client:build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "railway:build": "npm run install:all && npm run build",
    "railway:start": "cd server && npm run migrate:deploy && npm start"
  },
  "keywords": ["digital-signature", "contract-management", "docusign", "pdf"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}